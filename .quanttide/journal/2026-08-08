# 2026-08-08

## Studio mock 数据迁移 JSON seed + 发布 alpha.3

### 做了什么

- mock 数据从 `src/studio/lib/mock_data.dart`（Dart 字面量）迁移为 JSON seed：`src/studio/assets/data/seed_projects.json`
  - 结构：`{"projects": [...]}` 数组 + `id` 字段（替代原来中文名作 map key）
  - 枚举存键（`done`/`active`/`todo`、`implement` 等），解析时映射
  - 原定价估算注释保留为 `_comment` 字段（JSON 不支持注释）
- 模型：`Project` 增加 `id` 字段；12 个模型类新增 `fromJson`；枚举新增 `fromKey`
- 仪表盘：`rootBundle.loadString` 异步加载，加载中 spinner / 失败提示
- `pubspec.yaml` 注册 `assets/data/`
- 发布 `studio/v0.1.0-alpha.3`（tag + GitHub Release），CI 部署 data.quanttide.com

## 详情页组件分解 + 永久组件测试

- `project_detail_screen.dart`（1031 行）分解为 9 个共享组件到 `lib/widgets/`：sidebar / phase_tag / status_badge / section_header / toast / matrix_card / blueprint_card / timeline_card / doc_dialog，页面精简至 ~150 行
- `dashboard_screen` / `project_card` 去重：侧边栏、状态徽章、阶段标签改用共享组件
- `ItemStatus` 新增三态语义色扩展（color/badgeBg/badgeFg），矩阵与时间线的 todo/active 颜色归一（D1D5DB/E2E8F0 → 94A3B8，3B82F6 → 4F46E5）
- 新增 16 个永久 widget 测试（`src/studio/test/widgets` + `test/screens` + `test/helpers/seed.dart`），全部通过
- 清理 doc_dialog 旧原型 mock（TMA 商标场景文件映射、Assignor 跑数摘要）

## 发布 studio/v0.1.0-beta.1

- 组件重构 + 永久测试后发布 beta.1（tag + GitHub Release），CI 部署成功
- 线上验证：version.json = 0.1.0-beta.1，首页 200，seed 数据正常，无头 Chrome 启动无异常

## 发布 studio/v0.1.0-beta.2（PWA manifest 品牌化 + CDN 缓存修复）

- manifest.json：name/short_name 改为「量潮数据」，theme_color #4F46E5、background_color #F1F5F9
- deploy workflow：manifest.json 单独上传 `Cache-Control:no-cache`（原策略全站 max-age=31536000 一年，PWA 元信息变更无法生效）
- 线上验证：version.json = 0.1.0-beta.2；manifest 响应头 no-cache 生效（TCP_MISS 回源）
- 遗留提醒：index.html / flutter_bootstrap.js 同样被一年长缓存，入口更新依赖 CDN 目录刷新，建议后续一并 no-cache

### 关键发现

- widget 测试中 `tester.runAsync` + `rootBundle.loadString` 的 IO future 会悬挂导致 `pumpAndSettle` 卡死；测试内改用 `File` 同步读取 seed JSON（helpers/seed.dart）绕开
- 详情页 ListView 懒加载：测试需调大视口（`tester.view.physicalSize`）才能断言视口外卡片

### 关键发现/决策

- Flutter web 构建会把 pubspec 声明的资源放在 `build/web/assets/<path>` 下，即生产 URL 是 `/assets/assets/data/seed_projects.json`，`rootBundle` 加载的路径 `assets/data/...` 由引擎自动映射，无需改代码
- `flutter pub get` 在 CN 镜像环境会改写 `pubspec.lock` 的源（pub.dev → pub.flutter-io.cn），是环境噪音，提交前需还原
- alpha.2 的部署 workflow 曾失败（原因已由 e28e015 修复），alpha.3 部署成功

### 验证

- `flutter analyze` / `flutter build web` 通过
- JSON→模型解析与原文对齐（完成度 75%、矩阵 15 格、蓝图 7 步/4 异常、6 阶段 14 项）
- `https://data.quanttide.com/` 200，`/assets/assets/data/seed_projects.json` 内容正确
