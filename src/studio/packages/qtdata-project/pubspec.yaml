name: qtdata_project
description: 量潮数据看板组件
version: 0.1.0

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.5
  flutter_bloc: ^9.1.0
  quanttide_project: ^0.2.0
  flutter_quanttide_project: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
